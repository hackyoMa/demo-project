server:
  shutdown: GRACEFUL
  compression:
    enabled: true
    mime-types:
      - text/html
      - text/xml
      - text/plain
      - text/css
      - text/javascript
      - application/json
      - application/xml
      - image/vnd.microsoft.icon
      - image/svg+xml
      - font/woff2
  servlet:
    session:
      timeout: 60s
spring:
  application:
    name: demo-project
  main:
    banner-mode: off
  config:
    # Configuration file path
    import: optional:file:${FF_CONFIG_FILE:/opt/demo_project/config/conf.properties}
  threads:
    virtual:
      enabled: true
  servlet:
    multipart:
      max-file-size: 1024MB
      max-request-size: 2048MB
  messages:
    basename: i18n/messages
    fallback-to-system-locale: false
  datasource:
    type: com.zaxxer.hikari.HikariDataSource
  jpa:
    open-in-view: false
  liquibase:
    database-change-log-lock-table: db_version_lock
    database-change-log-table: db_version
logging:
  level:
    org.springframework.web.servlet.mvc.method.annotation.ExceptionHandlerExceptionResolver: ERROR
    org.eclipse.jetty.ee11.servlet.ServletChannel: ERROR
security:
  all-whitelist:
  whitelist:
    POST:
      - /user/_login
